<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="edu.tamu.csce315team11.weatherapp.ZipActivity"
    tools:showIn="@layout/activity_zip">

    <EditText android:id="@+id/enter_zip"
        android:layout_weight="1"
        android:textSize="80sp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:hint="@string/Zip"

        android:layout_marginTop="31dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <Button android:id = "@+id/enter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/enter"
        android:onClick="backWithZip"
        android:layout_below="@+id/enter_zip"
        android:layout_alignRight="@+id/enter_zip"
        android:layout_alignEnd="@+id/enter_zip" />

    <Button android:id="@+id/GPS"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/GPS"
        android:onClick="backWithGPS"
        android:layout_below="@+id/enter_zip"
        android:layout_alignLeft="@+id/enter_zip"
        android:layout_alignStart="@+id/enter_zip" />


</RelativeLayout>
